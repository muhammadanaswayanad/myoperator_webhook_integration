<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Instead of inheriting, create a separate settings view for MyOperator -->
    <record id="view_myoperator_config_settings_form" model="ir.ui.view">
        <field name="name">myoperator.config.settings.form</field>
        <field name="model">res.config.settings</field>
        <field name="arch" type="xml">
            <form string="MyOperator Settings" class="oe_form_configuration">
                <sheet>
                    <div class="oe_title">
                        <h1><field name="company_id" options="{'no_create': True}" invisible="1"/></h1>
                    </div>
                    <div class="o_settings_container">
                        <div class="o_setting_box">
                            <div class="o_setting_right_pane">
                                <div class="content-group">
                                    <div class="mt16">
                                        <label for="myoperator_webhook_url" string="Webhook URL"/>
                                        <div class="text-muted">
                                            Configure this URL in your MyOperator Dashboard for After-Call webhooks
                                        </div>
                                        <field name="myoperator_webhook_url" readonly="1" class="mt-2"/>
                                    </div>
                                    <div class="mt16">
                                        <label for="myoperator_webhook_secret" string="Webhook Secret"/>
                                        <div class="text-muted">
                                            If MyOperator provides a secret token for webhook verification, enter it here
                                        </div>
                                        <field name="myoperator_webhook_secret" class="mt-2"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </sheet>
                <footer>
                    <button string="Save" type="object" name="execute" class="oe_highlight"/>
                    <button string="Cancel" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Update the settings action to use our custom form -->
    <record id="action_myoperator_settings" model="ir.actions.act_window">
        <field name="name">MyOperator Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="view_id" ref="view_myoperator_config_settings_form"/>
    </record>
</odoo>
